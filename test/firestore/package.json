{
  "name": "jirani-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase --config ../../firebase.json emulators:exec --only firestore \"node --test chat_message_rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0"
  }
}
