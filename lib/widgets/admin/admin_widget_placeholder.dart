import 'package:flutter/widgets.dart';

class AdminWidgetPlaceholder extends StatelessWidget {
  const AdminWidgetPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
