import 'package:flutter/widgets.dart';

class AuthWidgetPlaceholder extends StatelessWidget {
  const AuthWidgetPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
