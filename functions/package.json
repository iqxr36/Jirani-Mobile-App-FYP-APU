{
  "name": "jirani-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && node --test test/*.test.js"
  },
  "dependencies": {
    "@google-cloud/documentai": "^9.6.2",
    "@google/genai": "^2.10.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0",
    "stripe": "^22.1.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
